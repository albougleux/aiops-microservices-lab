route:
  group_by: ["alertname", "service"]
  group_wait: 30s
  group_interval: 2m
  repeat_interval: 1h
  receiver: "aiops-agent-webhook"

receivers:
  - name: "aiops-agent-webhook"
    webhook_configs:
      - url: "http://ai-agent:5000/webhook"
        send_resolved: false
